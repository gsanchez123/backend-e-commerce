<div class="chat-container">
    <h2 class="chat-title">📢 Chat en tiempo real</h2>
    
    <div class="chat-box">
        <ul id="messages" class="chat-messages"></ul>
    </div>

    <form id="chatForm" class="chat-form">
        <input type="text" id="message" class="chat-input" placeholder="Escribe tu mensaje aquí..." required />
        <button type="submit" class="chat-btn">Enviar</button>
    </form>
</div>

<script src="/socket.io/socket.io.js"></script>
<script>
    const socket = io();

    const form = document.getElementById('chatForm');
    const messages = document.getElementById('messages');

    // 📢 Escuchar nuevos mensajes del servidor y mostrarlos en el chat
    socket.on('newMessage', (message) => {
        const li = document.createElement('li');
        li.classList.add('message-item');
        li.innerHTML = `<span class="message-text">${message}</span>`;
        messages.appendChild(li);

        // Hacer scroll automático hacia el último mensaje
        messages.scrollTop = messages.scrollHeight;
    });

    // 📩 Enviar mensaje al servidor
    form.onsubmit = (e) => {
        e.preventDefault();
        const input = document.getElementById('message');
        const message = input.value.trim();

        if (message) {
            socket.emit('sendMessage', message);
            input.value = ''; // Limpiar input después de enviar
        }
    };
</script>

<style>
    /* 🎨 Estilos mejorados para el Chat */
    .chat-container {
        width: 100%;
        max-width: 500px;
        margin: auto;
        padding: 20px;
        background: #f9f9f9;
        border-radius: 10px;
        box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
    }

    .chat-title {
        text-align: center;
        color: #333;
    }

    .chat-box {
        height: 300px;
        overflow-y: auto;
        padding: 10px;
        background: #fff;
        border-radius: 5px;
        border: 1px solid #ddd;
    }

    .chat-messages {
        list-style: none;
        padding: 0;
        margin: 0;
    }

    .message-item {
        padding: 8px;
        margin: 5px 0;
        background: #e0f7fa;
        border-radius: 5px;
        word-break: break-word;
    }

    .chat-form {
        display: flex;
        margin-top: 10px;
    }

    .chat-input {
        flex: 1;
        padding: 10px;
        border: 1px solid #ccc;
        border-radius: 5px;
    }

    .chat-btn {
        background: #007bff;
        color: white;
        border: none;
        padding: 10px;
        margin-left: 10px;
        cursor: pointer;
        border-radius: 5px;
    }

    .chat-btn:hover {
        background: #0056b3;
    }
</style>
